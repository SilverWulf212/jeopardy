DATABASE_URL=postgres://jeopardy:jeopardy@localhost:5432/jeopardy
PORT=3001
VITE_API_URL=http://localhost:3001
# Compose DB password (first init only; rotation needs ALTER USER, see README)
POSTGRES_PASSWORD=jeopardy
# Admin token for Manage writes (empty = open, dev only). Public deploys: set a long random value.
ADMIN_TOKEN=
# Umami analytics (optional — leave unset to disable)
# VITE_UMAMI_URL=https://umami.example.com
# VITE_UMAMI_WEBSITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
